<Window x:Class="Todo_List.Views.TaskEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edytuj zadanie" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tytuł" Grid.Row="0"/>
        <TextBox Text="{Binding Item.Title}" Grid.Row="0" Margin="60,0,0,0"/>

        <TextBlock Text="Data" Grid.Row="1" Margin="0,8,0,0"/>
        <DatePicker SelectedDate="{Binding Item.Date}" Grid.Row="1" Margin="60,8,0,0"/>

        <TextBlock Text="Opis" Grid.Row="2" Margin="0,8,0,0"/>
        <TextBox Text="{Binding Item.Description}" Grid.Row="3" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Zapisz" Width="80" Margin="0,0,8,0" IsDefault="True" Command="{Binding SaveCommand}"/>
            <Button Content="Anuluj" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
